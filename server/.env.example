# Server Configuration
# Port for the WebSocket server
PORT=3000

# Host binding address
# Examples:
#   0.0.0.0 - Allow access from network (recommended for multiplayer)
#   127.0.0.1 - Localhost only (more secure, but no network access)
HOST=0.0.0.0

# CORS Configuration (optional, comma-separated origins)
# Examples:
#   Allow specific origins: http://localhost:5173,http://192.168.1.100:5173
#   Allow all (dev only): Leave commented to allow all
# CORS_ORIGINS=http://localhost:5173,https://lingelo.github.io

# Node Environment
# Options: development, production
NODE_ENV=development
