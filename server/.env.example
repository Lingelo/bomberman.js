# Server Configuration
PORT=3000
# Bind to 0.0.0.0 to allow network access, or 127.0.0.1 for localhost only
HOST=0.0.0.0

# CORS Configuration (optional, comma-separated origins)
# CORS_ORIGINS=http://localhost:4200,https://lingelo.github.io

# Node Environment
NODE_ENV=development
